import ProfuctImage from "./ProfuctImage";

const ProfuctImageConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: 'product-image',
      element: <ProfuctImage />,
    },
  ],
};

export default ProfuctImageConfig;