import Oreder from "./Oreder";

const OrederConfig = {
  settings: {
    layout: {
      config: {},
    },
  },
  routes: [
    {
      path: 'oreder',
      element: <Oreder />,
    },
  ],
};

export default OrederConfig;